#include "email.h"

//email::email() : title(), subject(NULL), body(NULL), sender(NULL), recipiant(NULL), timeSent(0), containsVideo(false), attached(NULL) {}


//email::email(char* name, char* date, char* subject, char* body, char* sender, char* recipiant, double timeSent, bool containsVideo, const video & attached)//this is very long my apologies
//	: title(name, date), subject(subject), body(body), sender(sender), recipiant(recipiant), timeSent(timeSent), containsVideo(containsVideo), attached(attached) {}


